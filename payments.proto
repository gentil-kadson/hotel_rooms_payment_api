syntax = "proto3";

message Payments {
    int32 paymentId = 1;
    int32 bookingId = 2;
    int32 creditCardId = 3;
    string paymentStatus = 4;
    string createdAt = 5;
    string updatedAt = 6;
}

service PaymentsService {
    rpc GetAllPayments (Empty) returns (PaymentsList) {}
}

message Empty {}

message PaymentsList {
    repeated Payments payments = 1;
}